# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.*
# ...but the example must be committed: it documents which variables the app
# needs without carrying any real values. Negations must come after the rule
# they exempt, or git never sees them.
!.env.example

# Playwright
/test-results/
/playwright-report/
# The API suite writes its own report so the two do not overwrite each other.
/playwright-report-api/
/blob-report/
/playwright/.cache/
/playwright/.auth/
